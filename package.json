{
  "name": "Hypercharged",
  "private": true,
  "dependencies": {
  	"scripts": {
     "start" : "node app.js", 
     "test": "echo \"Error: no test specified                   \" && exit 1"
 }
  }
}
